#include <bits/stdc++.h>
using namespace std;

int main() {
    float radius, area;
    radius = 5.20;

    area = 3.1416 * radius * radius;

    cout << "The area is " << fixed << setprecision(3) << area;

    return 0;
}
